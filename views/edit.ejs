<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title></title>
    <link rel="stylesheet" href="/css/app.css" />
  </head>

  <body>
    <h1>Edit Fruit Page</h1>
    <!--  ADD action attribute to form tag-->
    <form action="/fruits/<%= fruit.id %>?_method=PUT" method="POST">
      Name: <input type="text" name="name" value="<%=fruit.name%>" /><br />
      Color: <input type="text" name="color" value="<%=fruit.color%>" /><br />
      Is Ready To Eat: <input type="checkbox" name="readyToEat" <%
      if(fruit.readyToEat === true){ %> checked <% } %> />
      <br />
      <select name='season'>
        <% for(let i = 0; i < seasons.length; i++) {  %>
          <option value='<%= seasons[i].id %>' ><%= seasons[i].name %></option>
        <% } %>
      </select>
      <br>
      <input type="submit" name="" value="Submit Changes" />
    </form>
  </body>
</html>
